This directory is only for bug reporting purposes.
It should NOT be part of ODL as is,
it should be reworked into a lightweight unit test instead.

This should compile.
Leafref (not via grouping) out of two layers of containers works.
Leafref out of one layer of containers works irrespective whether it comes via grouping.
